<body>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Username</th>
                <th>Email</t>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% data.forEach(item=> { %>
                <tr>
                    <td>
                        <%= item.id %>
                    </td>
                    <td>
                        <%= item.first_name %>
                    </td>
                    <td>
                        <%= item.last_name %>
                    </td>
                    <td>
                        <%= item.username %>
                    </td>
                    <td>
                        <%= item.email %>
                    </td>
                    <td><button type="button" onclick="window.location.href='/user/<%= item.id %>'">Info</button></td>
                </tr>
                <% }); %>
        </tbody>
    </table>
</body>